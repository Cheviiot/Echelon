# August 2026

## 15/08/2026

### Generated Russian localization bridge

- Added a data-driven `retail-russian-merge-v1` catalog delivery type for add-ons generated entirely on the user's machine. The repository publishes only the recipe and compatibility graph; no retail or third-party localization data is redistributed.
- Added bounded BIG and CSF readers to the launcher. The generator locates the required installed patch string table, reads `Data/English/generals.csf` from the user's `00RussianZH.big`, replaces every matching label, preserves unmatched mod-specific English text, and publishes the result through the existing transactional installer.
- Added native UTF-8 decoding to shared STR parsing while retaining byte-compatible fallback for legacy tables. Cyrillic and existing UTF-8 contributor names now reach `UnicodeString` correctly in both Generals and Zero Hour.
- Published `ROTR Russian Localization (Retail Bridge)` for HanPatch 32.2 in the independent Arsenal catalog. The add-on remains compatible with ordered companions such as Vanilla Campaigns Remastered because it is mounted as a normal selectable top layer.
- Added deterministic BIG/STR/CSF fixtures that generate 1,001 translated labels, install the resulting add-on, and verify that no matching English value survives. The launcher persistence suite and the central repository's 17 local checks pass.
- Accepted the deliberately empty `TOOLTIP:InvalidGameVersion` entry used by the real HanPatch STR while continuing to reject non-empty malformed sections. The regression fixture now preserves an unmatched empty entry through generation and transactional installation.
- Published catalog release `catalog-40a94966f8079ce0`, then installed the generated add-on from that unchanged live catalog through the packaged launcher. The catalog, byte-range resume, package resume, and private-state isolation checks pass against the public release. The user's HanPatch 32.2 table produced 8,770 entries, including 3,895 retail-matched translations, with all unmatched ROTR-specific labels retained as English fallbacks.
- Built and installed Flatpak commit `cc425ec2793ad2d4e8aa8de24dbfbac97480793022421c2788026b3c2b0cf180`. The installed engine mounted `ROTR -> HanPatch -> Controlbar -> Easter Eggs -> VCR -> Russian Localization`, parsed the UTF-8 STR, entered the main loop, and released the complete six-layer stack with `0x7f` quiescence.

### Rise of the Reds patch and add-on stack

- Published the first production extension graph in the independent Arsenal repository: HanPatch 32.2, the ROTR 18-button control bar, HanPatch Easter Egg Units, and Vanilla Campaigns Remastered. Every entry pins its exact parent or requirement, author-hosted URL, byte size, and SHA-256; the live catalog remains data-driven and requires no launcher rebuild.
- Corrected FinalBIG validation for the real HanPatch archives, including their exact directory trailers, while retaining rejection of unknown trailing data. Mounted managed BIG/GIB files in native SAGE precedence order within each layer and verified every winning archive entry.
- Replaced blanket add-on clearing with compatibility-aware stack reconciliation. Selecting HanPatch now preserves base-ROTR add-ons such as the control bar, while removing the patch removes only add-ons that actually require that patch.
- Preserved retail Windows INI semantics for deliberately oversized signed integer sentinels used by HanPatch. The shared Generals/Zero Hour parser now saturates them deterministically instead of terminating on `std::from_chars` overflow, and release diagnostics retain the exact INI error and source line.
- Hardened HTTPS delivery for extension packages. Dropbox normalization now preserves share keys, and origins that ignore a resumable Range request trigger an automatic verified restart from byte zero in the same operation instead of requiring another click or retaining appended corruption. Parallel file-set progress rolls back only the failed worker's contribution.
- Passed the private HTTPS integration suite for normal resume, changed ETags, ignored-Range fallback, and four-worker file sets. Installed the real HanPatch, control bar, Easter Egg Units, and 117 MB VCR add-on through the public GitHub catalog and the installed Flatpak.
- Launched the installed five-layer stack `ROTR -> HanPatch -> Controlbar -> Easter Eggs -> VCR`. All 18,130 indexed mod files resolved with verified precedence, the engine entered its loop, and VFS/DXVK/game teardown reported complete `0x7f` quiescence.
- Built and installed Flatpak commits `efa6b6a1dd10d0f0d34f8fa52f84e5e26b2eff5fa188defe7f0adcc6f031a9b6` for HanPatch runtime compatibility and `f31af7c3ed5c0d6704fed1c171bf463589d7054a27b2e4953567a5a7058ea75a` for robust non-Range downloads.

### Rise of the Reds launch compatibility

- Reproduced the installed ROTR launch failure against the real 18-file installation. The engine stopped while parsing `supplydroneairdropchinook.ini`, where the mod uses a 20-digit delay value as an effectively infinite unsigned duration.
- Traced the regression to the modern `std::from_chars` INI fast path: unlike the retail `%u` parser, it rejected positive literals above 32 bits. Preserved fast parsing and signed `-1` sentinels while deterministically saturating oversized positive unsigned values to `UINT32_MAX` in the shared Generals/Zero Hour parser.
- Passed native and installed-Flatpak launches with the user's ROTR profile. Both runs mounted all 15,183 indexed files, parsed the formerly fatal INI, created the main menu, entered the game loop, and released the engine with complete `0x7f` quiescence.
- Rebuilt and installed Flatpak commit `f4194f31d95435bf6e2084d51d8e685374f864481382417646d9320fe717a90f`. A graphical installed-package run also created a Vulkan swapchain in a private headless Wayland compositor without touching the user's desktop.

### Rise of the Reds onboarding foundation

- Researched the current Rise of the Reds ecosystem as a compatibility graph instead of treating every download as an interchangeable package: ROTR 1.87 Public Build 2.0 is the primary mod, HanPatch, AntiThesis, and ROTR Navy are mutually exclusive patch branches, and branch-specific add-ons require exact parent versions.
- Extended `RepositoryCatalogV1` and installed V2 manifests with source-qualified `Requires` and `Conflicts` selectors, including optional exact-version pins. Unknown, duplicate, self-referential, incomplete, and conflicting graphs are rejected before installation or VFS activation.
- Made saved mod profiles self-healing: reconciliation now removes stale or incompatible layers progressively, while every interactive selection and version change is rolled back if it would produce an invalid stack.
- Added direct read-only mounting for GenLauncher-style `.gib` BIG archives in both engine branches. Folder, archive, and standalone-file imports now validate `.gib` through the native BIG path; hashing, layer precedence, and release quiescence require no symlinks or retail-file mutation.
- Added a metadata-only ROTR candidate graph to the independent Arsenal repository. Candidates are validated but cannot enter the public catalog, reference package objects, or become downloadable until author redistribution permission and package verification are complete; executable-runtime-dependent add-ons remain blocked.
- Expanded settings, repository, manifest, exact dependency, conflict, profile-reconciliation, and cross-engine content-stack coverage. Both engines mounted the generated primary `.gib` layer followed by patch and add-on `.big` layers in exact precedence order and released all four layers cleanly.
- Built and installed Flatpak commit `b011cf4925cc5cbc6b5647d772f54615cf0559b8326d3dea88d7d63d5f566163`. The installed launcher loaded the live empty production catalog in a temporary HOME and private Wayland compositor, while both packaged engine modules continued to export only `GeneralsArsenal_GetEngineModuleV2`.

### Production Rise of the Reds installation

- Replaced the hidden ROTR candidate with a public Arsenal catalog entry backed by the author's existing HTTPS host. The independent repository pins the path, size, and SHA-256 of all 18 official ROTR 1.87 PB 2.0 files instead of mirroring third-party content.
- Extended `RepositoryCatalogV1` with bounded multi-file content sets. Unsafe or duplicate paths, executable components, missing hashes, zero sizes, non-HTTPS URLs, mixed delivery methods, excessive file counts, and aggregate content above 20 GiB are rejected before a card reaches the launcher.
- Added four-worker content downloading with independent resumable state per file, aggregate progress, completed-file reuse, mandatory per-file SHA-256 verification, and atomic publication only after the complete set passes native installation validation.
- Corrected the BIG validator for real ROTR archives: declared archive size is checked, valid empty tombstone archives are accepted, and the exact seven-byte `Lnnn` version trailer is recognized while unknown trailing data remains rejected.
- Removed the `Original Game` pseudo-card from the mod manager. Vanilla Generals and Zero Hour remain available only through the launcher's primary buttons; the manager now lists modifications exclusively.
- Added isolated installed-Flatpak repository QA that can select an engine and drive a real catalog installation without injecting input into the user's desktop. The test still renders through a private headless Wayland compositor and temporary HOME.
- Published central repository commit `42c01ff` and verified its live GitHub Release catalog, byte ranges, ETag behavior, and private-state isolation. The installed launcher loaded exactly one production entry, Rise of the Reds, from that unchanged remote catalog.
- Built and installed Flatpak commit `bc08d680c71c7eca30aef9e130fc7d95a97328c8e15469a59311107ac44db74a`. Its live installation downloaded all 18 files and 1,530,686,913 bytes, produced an 18-entry SHA-256 index and stable content fingerprint, left no staged files, and rendered ROTR as installed and active.

## 14/08/2026

### Central modification repository foundation

- Replaced normal GenLauncher network sources with one centrally administered Arsenal `RepositoryCatalogV1` endpoint. Old `Repositories.ini` files are ignored, while the legacy YAML reader remains isolated to offline migration coverage.
- Hardened native catalog ingestion around path-safe source/item/parent IDs, duplicate releases, known parent relationships, HTTPS-only metadata, and same-origin package/cover delivery. Catalog ModDB, Discord, news, and support links now survive normalization.
- Created the independent `/home/cheviiot/Project/Game/GeneralsArsenalRepository` project with a dependency-free atomic publisher, SHA-256-addressed immutable objects, non-destructive version removal, destination migration, and tamper verification.
- Published `Cheviiot/GeneralsArsenalRepository` as the free authoritative backend. Packages and covers use immutable content-addressed GitHub Release assets, while a complete catalog snapshot becomes visible atomically by marking only its release as `Latest`; no cloud subscription, storage credential, or database is required.
- Verified the real public release redirects, `HEAD`, byte ranges, strong ETags, correct `If-Match` success/failure behavior, private-state isolation, publisher suite, and rebuilt native Arsenal settings suite.
- Built and installed Flatpak commit `a40a015af79d801db35f80fd60d9547be90c27793558f49e823378e45303a6a6` for parallel testing. The installed package contains only the Arsenal catalog URL and completed two alternating engine handoffs with exact launcher-window restoration, independent engine presentation modes, its packaged font, and balanced 5/5 Wayland explicit-sync surfaces.
- Proved catalog/application independence end to end with that unchanged installed Flatpak: it first consumed the live empty catalog, then discovered a temporary mod, parent patch, and parent add-on after publication without a rebuild. The real package asset passed `HEAD`, byte-range, strong ETag, matching `If-Match`, and stale-precondition tests. The production empty catalog was restored before all temporary QA releases and objects were removed.
- Re-rendered the mod, patch, add-on, folder-action, and link-action manager pages in a private Wayland compositor after the source switch. All installed-launcher catalog checks ran under a private Wayland compositor and temporary home directory without using the user's desktop.

### Native mod manager checkpoint 4

- Added independent, atomically persisted Generals and Zero Hour selection profiles for one primary mod, one patch, and ordered add-ons. Missing or incompatible layers are reconciled without damaging unrelated global add-ons.
- Completed installed-version switching, active stack badges, the `Original Game` pseudo-profile, parent-aware patch/add-on filtering, mouse drag and `Shift+Up`/`Shift+Down` add-on ordering, and managed launch exclusively from the **MODS** screen.
- Added color-selected and cached grayscale card artwork, ten update flashes, custom cover replacement, full SHA-256 verification, side-by-side update retention, recoverable removal, and atomic restore.
- Split local installation into explicit **ADD FROM FILES** and **ADD FOLDER** workflows. Replaced ambiguous one-shot folder/link actions with modal menus for modification, game, maps, and replay directories plus trusted ModDB, Discord, news, and support links.
- Fixed the mod-details composition order that covered action buttons after drawing them. Private Wayland captures now show the complete control surface on Mods, Patches, Addons, folder-action, and link-action pages.
- Replaced lexical version ordering with numeric component comparison, preventing `10.0` from sorting below `2.0` and preventing older catalog entries from being offered as updates.
- Hardened HTTPS and S3 continuation around strong ETags and `If-Match`. Resumed transfers must receive HTTP 206; changed objects, ignored ranges, and precondition failures discard unsafe partial state rather than appending corrupt data.
- Added startup recovery for interrupted operations. Resumable downloads remain staged, abandoned imports are preserved for diagnosis, and only trash entries with valid installation manifests appear under **RESTORE**.
- Added native persistence/recovery coverage, a private TLS Range/ETag integration server, generated mod/patch/add-on UI fixtures, and isolated-Wayland screenshots. Both engines again passed exact four-layer VFS ordering and complete `0x7f` release.
- Built and installed the final checkpoint Flatpak commit `5b271e6e7c3422bdf0cea9f78aaf429ae9be8ea7e9ef125adaf4130bfab552cc`. The installed package mounted and released a verified four-layer stack in both engines, completed ten alternating window-preserving handoffs plus two additional post-UI-change handoffs, loaded its packaged font, restored independent engine presentation modes, balanced every Wayland explicit-sync surface, and exposes only `GeneralsArsenal_GetEngineModuleV2` from each module.

### Native mod manager checkpoint 3

- Replaced the private module contract with `GeneralsArsenalEngineHostV2` and the sole export `GeneralsArsenal_GetEngineModuleV2`. The host now passes ordered, immutable `GeneralsArsenalContentLayerV1` records and a stable SHA-256 fingerprint for the complete stack.
- Added a shared read-only content runtime to both engine branches. Relative loose-file reads and directory enumeration resolve the highest layer first, while writes remain confined to normal user-data paths.
- Mounted root BIG archives after retail assets in exact `base -> mod -> patch -> addons` order, enabled intentional later-layer replacement, and verify the live archive index points every managed file at the newly mounted archive.
- Added strict layer validation, canonical-root containment, case-insensitive path resolution, symlink escape rejection, duplicate prevention, maximum layer counts, and a dedicated content-release quiescence bit. Every return clears all managed paths before the launcher can reuse its window.
- Added launcher CLI selection for one mod, at most one patch, ordered repeatable addons, and explicit `--no-mods`. Managed stacks and legacy `-mod` are mutually exclusive, and headless/replay sessions never inherit UI state.
- Added mandatory launch-time reindexing. Changed, truncated, symlink-injected, or executable-bearing installations are rejected before module loading; BIG headers, directory bounds, paths, offsets, sizes, and embedded executable names are validated before the legacy parser sees an archive.
- Connected **LAUNCH** on installed primary-mod cards to the managed VFS path while keeping the top-level Generals and Zero Hour buttons strictly vanilla.
- Added a reproducible content-stack smoke test with valid generated BIG fixtures, duplicate loose paths, four ordered layers, both engines, tamper rejection, legacy-argument conflict, stable fingerprints, real BIG precedence, and complete `0x7f` teardown.
- Passed the standalone suite, both native headless profiles, four alternating private-Wayland handoffs, and the full managed-stack test in Generals and Zero Hour. Both shared objects export only the V2 entry point.
- Built and installed Flatpak commit `ffdeb4f9a66f03eeb3cdfb0db16332bff3b8b0a406f4d1ecda981c2c5f61f55f`. The installed package completed two alternating window-preserving handoffs and launched a verified four-layer Zero Hour stack with packaged V2 modules.

### Native mod manager checkpoint 2

- Added asynchronous GenLauncher-compatible Generals and Zero Hour catalog refresh with a 24-hour cache, offline fallback, source-qualified IDs, cached covers, and simultaneous support for strict user-configured `RepositoryCatalogV1` sources.
- Added native HTTPS package transport with Dropbox/OneDrive normalization, resumable staging, remote size checks, optional mandatory catalog SHA-256, cancellation that preserves partial data, and atomic handoff to the content installer.
- Added an independent S3 ListObjectsV2/Range/SigV4 transport. Credentials are read only from protected Arsenal configuration or environment variables, never copied from the decompiled launcher, never logged, and never sent to an endpoint without TLS.
- Pinned yaml-cpp 0.9.0 and libarchive 3.8.9 and added their Flatpak source inputs and licenses. ZIP, 7z, RAR, BIG, folder, and legacy directory imports share one clean-room installation path.
- Added safe extraction and publication limits: path traversal, absolute paths, symlinks, hardlinks, devices, executable components, excessive file counts, expansion bombs, incorrect sizes, and incorrect SHA-256 values fail before publication.
- Added deterministic per-file SHA-256 indexes and content fingerprints, side-by-side versions, recoverable `.trash` removal, persistent `.staging` operation journals, multi-file import, visible progress, and safe stop behavior.
- Connected available catalog cards to **INSTALL**, installed cards to recoverable **REMOVE**, and kept managed **LAUNCH** disabled until the ABI V2 overlay exists, preventing any temporary retail-file mutation.
- Extended the standalone suite for both YAML formats, custom Arsenal sources, V1 migration, source collisions, safe imports, cancellation, metadata injection, symlinks, traversal, forbidden binaries, checksum failures, atomic publication, verification, and recoverable removal.
- Built the launcher and both engine modules in `dev-ubuntu`, passed the native and packaged suites, and installed Flatpak commit `3ac1e8aeece9792587e3093625f5413db94c1774a016d4106d78f9457a9c71fb` for parallel manual testing.
- Launched the installed package with an isolated HOME in a private headless Wayland compositor, verified the packaged font and `MODS` manager at 1280x800, and confirmed that the background catalog refresh does not block rendering or shutdown.

### Native mod manager checkpoint 1

- Inserted **MODS** as the third main-menu action and added a full-window emerald manager workspace with independent Generals/Zero Hour and Mods/Patches/Addons tabs.
- Added native installed-content cards with the GenLauncher-compatible 500x100 artwork geometry, selected color artwork, cached grayscale inactive artwork, keyboard/mouse selection, scrolling, details, refresh, and the final action-bar layout.
- Replaced the single-path installed-mod model with source-qualified `ModificationManifestV2` records covering mods, patches, addons, parents, sources, hashes, fingerprints, and cover paths. Same-name entries from different sources remain independent.
- Added atomic V1-to-V2 manifest publication while preserving the original manifest, path containment, transient staging/trash/cache exclusion, catalog bounds, and standard per-engine/per-type installation directories.
- Removed the obsolete active-modification field from launcher settings and engine-profile UI. The Generals and Zero Hour buttons now always launch clean profiles; managed content remains isolated to the mod manager.
- Added `--mods` direct manager startup and expanded the standalone persistence suite for V2 migration and source-ID collision handling.
- Built the launcher and both engine modules with GCC 15 inside the Flatpak 25.08 SDK, passed the packaged settings suite, and installed Flatpak commit `1f66ccddbe34659b25faeabaa0693f7a6dcfbc690d754e32ce087f9a2b5ac397` for parallel manual testing before graphical QA.

### Live-shell quick start

- Split the shared `-quickstart` behavior from `-noshellmap`. Quick start now skips startup movies, logos, and shell-window animations while keeping the live animated shell map in both Generals and Zero Hour.
- Kept the static menu background as an explicit independent choice. Combining **Quick start** with **Disable shell map** reproduces the previous fast static-background behavior.
- Added launcher argument regression coverage and a private debugger-driven Wayland test that proves both branches reach `TerrainLogic::loadMap` without calling either EA or Sizzle intro movie function.
- Updated the command-line, SagePatch, launcher, and QA-script documentation to describe the two independent controls.
- Built the launcher and both engine modules, passed the settings suite and live-shell test, then built and installed Flatpak commit `38b38f94a6be067175743a64ad07042019c3ef5b73b50875a913f89536f07547` for parallel manual testing.

### Independent launcher and engine presentation modes

- Replaced the passive host-owned window policy with an explicit launcher-to-engine presentation protocol. Both engines now request windowed/fullscreen changes and render dimensions through the private module ABI while the launcher remains the sole SDL window owner.
- Applied the selected engine presentation before DXVK device creation and routed later SAGE display-mode changes through the same coordinator. Failed device resets roll the physical window back together with the engine's logical display state.
- Captured the complete launcher presentation before every session and restored its mode, logical/pixel size, and maximized state only after the engine reaches the full quiescence contract. X11-compatible backends also restore placement; Wayland correctly leaves placement to the compositor instead of treating its protocol restriction as a fatal error.
- Extended native and installed lifecycle QA to require three state samples per handoff, exact launcher mode/size restoration, and observed alternating engine requests for `800x600` windowed and `1024x768` fullscreen presentations.
- Passed native isolated-Wayland coverage with both windowed and fullscreen launchers, then passed the same two combinations with the installed Flatpak. A final ten-session installed stress run restored the launcher after every alternating engine handoff and balanced 21/21 explicit-sync surfaces.
- Confirmed that both private modules still export only `GeneralsArsenal_GetEngineModuleV1`, passed settings persistence, and installed Flatpak commit `ad11cb3f64c7ef5fdec735ed6be67d45203d9ef89a40508c923de113fd11d554` for parallel manual testing.

### Focused launcher preferences

- Removed the remaining **Game data folder / Select** row from the launcher settings page, including its mouse and keyboard action, so the page now contains only language, display mode, and window size.
- Kept the verified folder selection and migration path exclusively in automatic first-launch setup, and replaced the obsolete cancellation instruction that pointed users back to Settings.
- Updated the launcher guide to match the reduced three-row page.
- Built and installed Flatpak commit `0157ee091509d3eefe98ee06ab8300fec768d17c32c29cc3f0f2471a55dd656f` before extended QA, so the corrected UI was immediately available for parallel user testing.
- Captured the installed launcher page in private headless Wayland and confirmed exactly three rows with the packaged font, passed the standalone settings transaction suite, and completed two alternating installed-engine handoffs with unchanged shared-window geometry and balanced 5/5 explicit-sync surfaces.

### Seamless launcher/engine window ownership

- Made the launcher authoritative for the shared SDL window's position, logical/pixel size, fullscreen state, and maximized state throughout in-process engine sessions.
- Added a private host window policy to the module ABI and applied it before WW3D/DXVK initialization in both Generals and Zero Hour.
- Separated SAGE's configured render resolution from DXVK's presentation surface: hosted engines keep their own virtual resolution and pillarbox into the existing launcher-owned window.
- Disabled both legacy `SetWindowPos` resizing and the later SDL fullscreen/window-size transition only for hosted sessions; standalone engine targets retain their existing behavior.
- Removed the launcher's unconditional fullscreen-off, resize, fullscreen-on sequence after engine teardown. Display settings are now applied only when the user changes them.
- Added before/after handoff geometry telemetry and made the isolated lifecycle tests reject changes to position, logical size, pixel size, fullscreen state, or maximized state.
- Built the launcher and both engine modules in `dev-ubuntu`, passed the settings suite, then completed ten alternating handoffs plus separate mismatched-resolution windowed and fullscreen runs in private headless Wayland. Window state remained identical in every transition and explicit-sync objects remained balanced.
- Built and installed Flatpak commit `cf76bef5d0361338f3e5e3229f354c70a5fe2a29f7834d8c98e4d37ccce5596e`; four installed alternating handoffs preserved the same window geometry and balanced 9/9 explicit-sync surfaces.

## 13/08/2026

### Launcher display, localization, and installed-mod foundation

- Added persistent launcher-owned windowed/fullscreen mode and window-size presets, applied on startup and restored after every engine-to-launcher handoff.
- Added independent Russian BIG localization switches for Generals and Zero Hour. The host now publishes an exact archive-name policy and both VFS backends skip the selected localization layers without renaming or modifying retail files.
- Confirmed the installed retail names are `00Russian.big` and `00RussianZH.big`; compatibility spellings are accepted while prefix matches are rejected.
- Introduced a local `ModificationManifestV1` catalog inspired by the useful model in the decompiled GenLauncher research, with engine/version separation, path containment, manifest size limits, duplicate rejection, and native `-mod` launch integration.
- Added an active-modification selector to both engine settings pages. Network installation, updates, and removal remain intentionally outside this first offline foundation.
- Extended the standalone settings test for display persistence, localization policy, safe mod discovery, engine isolation, and exact BIG matching.
- Built both engine modules and the launcher, passed the settings suite, rendered the expanded launcher settings page in private headless Wayland, completed two alternating engine handoffs, and verified that Generals skips `00Russian.big` while Zero Hour skips both `00RussianZH.big` and the base `00Russian.big`.
- Built and installed Flatpak commit `32013ca97384c704617cbb1fc53526ecc43b4a55433b4d35e98f1a92be8fe8e1`; the installed application passed a private Wayland handoff with its packaged font and balanced 3/3 explicit-sync surfaces.

### Emerald launcher HUD

- Replaced the mixed Generals-orange, Zero Hour-blue, and cyan launcher accents with one centralized saturated emerald palette while preserving the original SAGE button state and transition behavior.
- Applied the Arsenal palette consistently to the outer frame, grid, panels, buttons, hover flashes, settings rows and values, tabs, scrollbar, progress overlays, and non-error status text. Red remains reserved for actual errors.
- Kept the orange-and-steel Arsenal wordmark as brand artwork rather than treating it as an interactive HUD element.
- Removed the game-data picker from the top navigation, expanded the three real settings tabs across the available width, and retained the operation as a functional **Game data folder / Select** row inside the launcher page.
- Delayed deterministic main-menu screenshots until the SAGE button entrance animation has settled, while settings captures remain immediate.
- Built the launcher, passed the standalone settings test, captured the main and settings screens at 1280x800 in private headless Wayland, and completed two alternating engine handoffs with complete quiescence and balanced explicit-sync surfaces.
- Rebuilt and installed Flatpak commit `3e4ffb2c42aabd7f29c4e18c516644db1ac31f296ad04db0754e175babc8e455`; its packaged launcher completed a private Wayland handoff with the bundled font and balanced explicit-sync surfaces.

### Generals quick-start parity

- Confirmed that `-quickstart` is implemented by the shared command-line parser and only disables startup logos, the shell map, and window animations; it has no Zero Hour-specific dependency.
- Removed the launcher-only Zero Hour restriction, so the Generals settings page now exposes and persists the same quick-start switch.
- Simplified profile argument assembly around the shared capability and retained case-insensitive duplicate suppression for explicitly supplied arguments.
- Extended the settings test to cover Generals persistence, argument emission, and explicit-argument precedence.
- Built the launcher, settings test, Generals, and Zero Hour targets, then ran both headless profiles successfully. A direct Generals `-quickstart` run skipped `ShellMap1` and reached the complete `0x3f` quiescence state.
- Rebuilt and installed Flatpak commit `818af8da68f50a01117ca2b9f968ba96fda5992adc71e062e0aca8f1171cd6bf`, verified its normal Wayland lifecycle, and confirmed that a saved Generals `QuickStart=yes` reaches the packaged engine, suppresses `ShellMap1`, and returns safely in a private compositor.

### GeneralsX Beta 16 synchronization

- Created `generalsx-sync-08-13-2026` from the fork base, fetched `fbraz3/GeneralsX`, and merged `upstream/main` at `70cccbdc3` without rewriting shared history.
- Integrated 29 non-merge upstream commits, including the GeneralsX Beta 16 tag, macOS HiDPI placement, shared W3D/Win32/GUI moves into `Core`, aircraft and transport fixes, sticky bomb and special-power fixes, LAN chat improvements, and the revised project watermark and update checker.
- Reapplied the complete Arsenal overlay after the merge. Git resolved the engine and launcher intersections cleanly; the shared August worklog was combined manually and all intentionally removed legacy GeneralsX packaging files remained removed.
- Preserved the upstream watermark and update-checker behavior while routing the universal build through the centralized Arsenal identity. Product strings, tag-prefix handling, API requests, user agent, update links, and the test override now use Generals: Arsenal and its release channel.
- Audited incoming gameplay changes for direct additions of platform math functions covered by the determinism policy; none were introduced.
- Reconfigured from scratch and compiled the launcher, both engine modules, both monolithic debug targets, and the standalone settings test in `dev-ubuntu` against the unified upstream `Core` layout.
- Verified both modules still export only `GeneralsArsenal_GetEngineModuleV1`, both headless profiles complete, the native `RETURN TO ARSENAL` row remains present in both games, and the settings transaction suite passes normally and under AddressSanitizer/UndefinedBehaviorSanitizer.
- Completed ten alternating native handoffs followed by three post-branding handoffs in private headless Mutter. Explicit-sync surfaces balanced 21/21 and 7/7, subsystem quiescence remained complete, and thread/file-descriptor counts stayed stable.
- Built the Flatpak with the 25.08 SDK and GCC 15, executed the packaged settings test, then rebuilt after the final case-insensitive Arsenal tag-prefix adjustment and installed commit `cb648ac1d887d44a13916b3944f0ba4122ec2680ac87f3c265c63c9aaa3aa40b`.
- Completed three installed-Flatpak handoffs with the packaged font and 7/7 balanced explicit-sync surfaces in a private Wayland compositor.
- A retail replay fixture was not available in the checkout or local Arsenal data tree, so replay/CRC execution remains an explicit follow-up rather than being reported as a synthetic pass.

### Launcher settings foundation

- Audited the decompiled Windows GenLauncher in `/home/cheviiot/Project/Game/Research` and separated useful user-facing features from unsafe file swapping, direct BIG patching, unsigned updates, MD5-only verification, and platform-specific process behavior.
- Replaced the data-folder-only Options action with full SAGE-styled settings pages for Generals, Zero Hour, and the launcher, including mouse, keyboard, scrolling, defaults, cancel, and apply behavior.
- Added independent launch options and native `Options.ini` graphics settings for both engines. Unknown INI entries and duplicate known entries are preserved consistently instead of replacing the entire retail preference file.
- Added camera, scroll, terrain draw distance, and frame-rate controls through each profile's supported `SagePatch.ini`; retail BIG archives are never patched.
- Added shell-free argument tokenization, validation before publication, and command-line precedence over saved switches. Headless and replay dispatch continue to bypass graphical launcher preferences.
- Made the five settings files a recoverable transaction with staging, backups, a journal, rollback on failure, and startup recovery after an interrupted publication.
- Added a standalone settings test covering validation, round trips, unknown-key preservation, Windows path parsing, cross-profile publication, and interrupted-transaction recovery.
- Wired the standalone settings test into the Linux Flatpak build workflow so upstream merges cannot silently break persistence.
- Added deterministic launcher screenshot hooks for isolated QA and visible page navigation for the 25 per-game settings.
- Built the launcher and settings test in `dev-ubuntu`; the normal, AddressSanitizer, and UndefinedBehaviorSanitizer runs pass.
- Captured the top and bottom settings pages in private headless Mutter/Wayland sessions at 1024x768, 1280x720, 1280x800, and 1600x1000 without overlap.
- Verified direct headless dispatch for both profiles and three alternating graphical engine handoffs with complete quiescence reports and balanced Wayland explicit-sync surfaces.

### Wayland explicit-sync teardown root cause

- Captured the real GNOME Wayland failure without accessing the host display. Mutter rejected a second `wp_linux_drm_syncobj_surface_v1` for the launcher window because the previous DXVK session had not destroyed its surface object.
- Fixed the underlying `DX8Wrapper` shutdown order. Wrapper-owned cached textures and render-state resources are now released before the D3D8 device and before `CurrentCaps` is deleted; the old post-device cleanup was unreachable.
- Added the final D3D8 COM release count to the engine quiescence contract, preventing a detached pointer from being reported as a destroyed DXVK device.
- Made a fatal SDL Wayland disconnect exit the UI worker with the recoverable graphics code immediately, before SDL's synthetic quit event can be mistaken for a normal application exit.
- Added worker PID diagnostics and retained bounded clean-worker recovery in the windowless supervisor.
- Strengthened native and installed-Flatpak lifecycle tests to capture the Wayland protocol and reject duplicate, unbalanced, or missing explicit-sync surface destruction.
- Completed ten alternating Generals/Zero Hour sessions in private headless Mutter. All sessions reported the `0x3f` quiescence mask, a final D3D8 reference count of zero, and 21/21 explicit-sync surface balance.
- Rebuilt and installed Flatpak commit `e23bdadf669bde7003f412373a31e9cadc0e7c7389bceaa5ee8f0debd42a67d2`, repeated the ten handoffs, and verified supervisor recovery after abrupt worker termination.

### Fault-tolerant launcher return lifecycle

- Replaced implicit engine teardown assumptions with an explicit versioned lifecycle and quiescence report across the launcher ABI.
- Added complete subsystem singleton ownership records. Shutdown now deletes subsystems in reverse order and clears the exact global singleton after each deletion, eliminating stale pointers during a later engine session.
- Added the missing Generals `GameLODManager` release and a cross-branch audit that verifies all engine-owned singleton pointers are null before the window can return to the launcher.
- Made SDL/Vulkan handoff transactional: the launcher destroys its renderer before engine entry, synchronizes the window around DXVK teardown and fullscreen changes, validates the first restored Vulkan present, and refuses unsafe reuse.
- Added a windowless supervisor around the graphical UI worker. Fatal Wayland/Vulkan failures now restart a clean launcher instead of closing the whole application, with a bounded recovery rate and a visible recovery message.
- Kept headless and replay dispatch outside the supervisor and graphical launcher path.
- Added isolated Wayland lifecycle, supervisor-failure, and headless-dispatch QA scripts. The graphical scripts explicitly remove access to the host display.
- Completed two 20-session alternating runs, verified stable quiescent resources, direct headless startup for both profiles, supervisor recovery, one-symbol module export tables, and ten installed-Flatpak handoffs.
- Added an offline Flatpak rebuild mode for pinned sources after the source cache has been populated.

## 12/08/2026

### Generals: Arsenal rebrand

- Rebranded the universal product as **Generals: Arsenal** while retaining the upstream `Generals`, `GeneralsMD`, `Core`, `g_generals`, and `z_generals` structure.
- Added centralized CMake and C++ brand identities, the `GeneralsArsenal` executable, the private `GeneralsArsenal_GetEngineModuleV1` ABI, and `GENERALS_ARSENAL_*` runtime interfaces.
- Changed the canonical data root to `~/.GeneralsArsenal` and removed automatic discovery or migration of GeneralsX roots.
- Added the transparent `GENERALS` / `ARSENAL` launcher wordmark and square Arsenal monogram, with aspect-preserving SDL texture rendering and a text fallback.
- Renamed the in-engine launcher action to **RETURN TO ARSENAL** in both game branches.
- Replaced the split Linux packages with `io.github.cheviiot.GeneralsArsenal`, containing the launcher and both private engine modules.
- Renamed the GitHub fork to `Cheviiot/GeneralsArsenal` and retained `fbraz3/GeneralsX` as the dedicated `upstream` remote.
- Added the reviewed GeneralsX merge workflow, reusable sync prompt, local Git `rerere` configuration, and a weekly read-only upstream status check.
- Reworked README and HOWTO documentation to identify Arsenal as a GPL GeneralsX fork with full upstream attribution.
- Built the launcher and both engines, verified one-symbol module exports, clean-HOME behavior, the Vulkan-rendered launcher, the unified Flatpak, the available retail replay, metadata, workflows, and exact upstream ancestry.

## 11/08/2026

### Universal GeneralsX launcher

- Added the versioned `GenXEngineHostV1` / `GenXEngineModuleV1` C ABI with the single `GeneralsX_GetEngineModuleV1` module export.
- Added private Generals and Zero Hour runtime modules, hidden symbol visibility, Linux export maps, and a universal `GeneralsX` SDL3 host.
- Kept the existing monolithic engine targets and added `RTS_BUILD_UNIVERSAL_LAUNCHER` as a release opt-in.
- Added direct profile selection, headless bypass, procedural RU/EN UI, unavailable-profile handling, data-folder selection, localization discovery, profile files, and verified legacy migration.
- Added the return action as a native seventh row in both retail main-menu layouts, inheriting the original button styling, transitions, and visibility behavior without modifying proprietary WND files.
- Added the original distributable launcher backdrop, SAGE push-button behavior, centralized normalized layout, and full-window scaling for 4:3, 16:9, and 16:10.
- Built the launcher and both modules, verified their export tables, clean-HOME behavior, profile dispatch, native menu integration, and multiple launcher resolutions.

### Arsenal development environment

- Main checkout: `/home/cheviiot/Project/Game/GeneralsXUniversal`
- Distrobox container: `dev-ubuntu`
- GUI tests use private headless Mutter/Wayland sessions only; the host display is never accessed.

## 11/08/2026
### Upstream Sync from TheSuperHackers (`thesuperhackers/main`)
- Created sync branch `thesuperhackers-sync-08-10-2026` and merged upstream `thesuperhackers/main`.
- Resolved logic conflicts in `GameLogicDispatch.cpp` (preserving upstream timing checks alongside local performance optimizations) and `Recorder.cpp`.
- Resolved build system conflicts in `CMakeLists.txt` across `Core/GameEngineDevice`, `Generals/`, and `GeneralsMD/` while preserving platform isolation (`if(WIN32)` blocks).
- Reconciled file movements into `Core/`: applied local custom fixes (`shouldPause` HUD behavior, GeneralsX watermark, Cyrillic font support) to the unified `Core/` classes (`InGamePopupMessage.cpp`, `W3DMainMenu.cpp`, `W3DGameFont.cpp`).
- Verified full compilation and linking on macOS (`macos-vulkan` preset) for Zero Hour and Generals base game.
- Executed full Linux Docker configure (`docker-configure-linux.sh linux64-deploy`) and build scripts (`docker-build-linux-zh.sh` and `docker-build-linux-generals.sh`), confirming clean compilation and linking of `GeneralsXZH` (280M) and `GeneralsX` (268M) binaries.

### Fix Multi-Monitor Window Placement and HiDPI Window Sizing
- Fixed the game opening on a secondary monitor at an oversized width on multi-monitor macOS setups.
- Root cause was a unit mismatch: `buildFilteredResolutions()` clamps the resolution list against `SDL3_GetNativeDisplaySize()`, which returns `mode->w * pixel_density` (physical pixels), but `SDL3_ApplyWindowModeForRenderConfig()` passed those values straight to `SDL_SetWindowSize()`, which expects logical points. On a 2x Retina display a 3840x2160 render resolution produced a 7680-physical-pixel-wide window — twice the width of the whole desktop.
- The oversized window then spanned both displays, and because SDL resolves a window's display by its center point, `SDL_GetDisplayForWindow()` reported the adjacent monitor and the window migrated there.
- `SDL3_ApplyWindowModeForRenderConfig()` now derives the pixel-to-point ratio from the window itself (`SDL_GetWindowSizeInPixels` / `SDL_GetWindowSize`) rather than the display's `pixel_density`. The drawable is only scaled when the window carries `SDL_WINDOW_HIGH_PIXEL_DENSITY`, which is set on macOS only, so the ratio collapses to 1.0 on other platforms and non-HiDPI behavior is unchanged.
- `SDL_CreateWindow()` takes no position argument and resolves to `SDL_WINDOWPOS_UNDEFINED`, which SDL maps to display index 0 of its own enumeration order rather than the OS primary display. `SDL3Main.cpp` now pins the initial placement to `SDL_GetPrimaryDisplay()` while the window is still hidden, so there is no visible jump and fullscreen no longer inherits the wrong display.
- Added `SDL3_CenterWindowOnCurrentDisplay()`, called after the window is resized. SDL keeps the top-left anchor across `SDL_SetWindowSize()`, so growing from the 1024x768 bootstrap size to the configured resolution previously pushed the window off the bottom and right of the display. Centering is clamped to `SDL_GetDisplayUsableBounds()` so the window stays clear of the macOS menu bar, and anchors to the bounds' top-left when the window is larger than the usable area, which keeps the title bar reachable.
- Positioning failures are logged rather than swallowed; Wayland rejects programmatic window positioning by design, so this is a warning and not an error.
- Applied to both Generals and Zero Hour.

### Known Remaining Issue
- `DX8Wrapper::Resize_And_Position_Window()` (`dx8wrapper.cpp`) hardcodes position `(0,0)` on SDL platforms where the Win32 path centers on the current monitor's work area, and passes physical pixels to the `SetWindowPos` compat shim, which forwards them to `SDL_SetWindowSize()`. The window-mode pass in `W3DDisplay` corrects both afterwards, so the visible result is right, but the window can briefly flash at the wrong geometry. Fixing it at source touches shared `Core/` code and needs Linux validation before it lands.
