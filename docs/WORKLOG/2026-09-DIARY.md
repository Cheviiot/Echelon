# September 2026

> [!NOTE]
> **AI-Generated Content Disclosure**: This worklog is automatically generated and maintained by AI coding agents to document daily progress, debugging sessions, and technical decisions.

## 07/09/2026

### Finish the hosted session refactor

- Replaced the ABI V3 blocking adapter with a native `Create → Prepare → Start → Step → Stop → Quiescent → Destroy` frame lifecycle. The launcher pumps frames until the engine requests stop and always executes the teardown path after a failed frame.
- Reconfigured the immutable content stack at native session start, before engine initialization, so prepared workspaces remain part of the actual V3 runtime and cannot silently fall back to vanilla content.
- Kept ABI V2 and the legacy `GameMain` path intact. The V2 host prefix remains binary-compatible, while the appended policy tail is optional. V3 receives asset, user-data, language and archive policy through the host payload, avoids changing the process cwd, and leaves environment mutation only to the scoped V2 compatibility bridge.
- Added structured diagnostics for workspace publication, content import operations and engine sessions: stage, numeric code, profile, workspace, affected path and retryability are emitted with every operation result.
- Extracted the renderer-neutral `PresentationService` from the launcher monolith. It owns only the engine window-mode callback; SDL/Vulkan renderers remain separate and no graphics-device handles cross the boundary.
- Updated Wayland QA fixtures to write the mandatory `Launcher/Settings.v3.ini` schema. The native headless dispatch, both-engine nested-archive/content-precedence smoke and CTest suite pass inside `ubuntu-dev`.
- The host-side Wayland run was not used as a product result because the host cannot resolve the container-built FFmpeg shared libraries; the same scenario reaches the launcher worker and reports the missing module path explicitly.
- Refactored BIG-file lookup, user-data initialization, language detection and launcher return localization to prefer the explicit hosted context. Legacy environment names remain fallback paths for standalone and V2 compatibility.
- Hardened interrupted-operation recovery: journals now carry the owner PID and terminal failure state, live imports remain in staging, and abandoned workspace staging is quarantined only after an age check.
- Kept an existing published workspace active until its replacement has been fully materialized and recorded; failed publication now attempts to restore the previous generation.
- Refined the content model into typed manifests, resolved snapshots and file records; imported `files.sha256` entries now carry relative paths, sizes and SHA-256 values and are checked again at launch.

## 06/09/2026

### Strengthen the launcher content and session boundary

- Added atomic directory locks for the launcher instance, game profiles, installations, imports, restores and workspace preparation. Locks record the owner PID, recover abandoned operations after a process exit, and leave active work owned by another process untouched.
- Added verified content workspaces with generation records, SHA-256/fingerprint checks, isolated materialized layer copies, quarantine of invalid records and idempotent reuse after a clean retry.
- Added the `LegacyBlockingEngineSession` host adapter with explicit prepare, run, stop and quiescence states while retaining the existing ABI V2 engine entry point.
- Moved launcher persistence to a clean `Settings.v3.ini` schema and kept settings publication transactional; malformed or old schemas fall back to defaults without partially replacing the active file.
- Kept launcher SDL resources alive through preflight, routed profile launches through the workspace and lock checks, and restored the launcher presentation only after engine shutdown reports quiescence.
- Added regression coverage for lock exclusion, versioned settings publication, workspace reuse and recovery ownership. Native per-frame V3 stepping and the shared renderer bridge now run through the launcher session boundary.
- Workspace materialization now uses hardlinks only for regular files with no write permission bits and falls back to isolated copies for mutable or incompatible files. The original installation remains a read-only source.
- Refactored the workspace record parser to require indexed, non-duplicated layer keys, reject malformed generations and quarantine invalid publications. Reuse now compares every available layer fingerprint with the selected stack before the engine receives a path.
- Refactored managed-content validation into one preflight/handoff path, removed duplicate legacy `-mod` checks, and made settings staging names unique per publication. Recovery rejects path-like transaction identifiers and stops before destructive cleanup when filesystem inspection fails.
- Added tampered-workspace and malformed-transaction regressions. The verification build, CTest, Echelon boundary check and upstream ownership check pass after the refactor.
- Exported the additive V3 session entry from both Linux engine modules and made the product build track the linker export map; macOS receives the same symbol through the product-side target bridge. V3 advertises ABI version 3 while the legacy run accepts both V2 and V3 host payloads.
- Hardened lock release with per-acquisition owner tokens so a process that lost a stale-lock race cannot remove the replacement process's lock. The lock regression suite remains green.

### Complete the owner-authorized merge

- The owner explicitly requested immediate integration without further checks, deferring remaining validation to later work. Merge all prepared fixes and the Russian README into `main`, preserve the locked original `upstream`, then stop.
- Compilation and local-content tests passed on macOS; full application packaging remains unverified after the latest dylib-identity correction. No release readiness or replay compatibility is claimed.

### Distinguish dylib identity from dependencies

- Inspection of the macOS packager found that `otool -L` reports the library's own install name as well as dependencies. Searching for that identity can incorrectly choose a system copy even when the original library is already selected.
- Excluded `LC_ID_DYLIB` entries from dependency traversal while keeping all actual load dependencies and duplicate-library rejection intact.
- Added portable regression fixtures for a dylib with an identity and for executable/module load dependencies. Both pass locally and run in the macOS packaging job.

## 05/09/2026

### Share SDL3 with the macOS DXVK build

- CI 33968601862 passed all compilation targets and macOS local-content tests, then correctly rejected two different SDL3 libraries during application packaging: the fetched engine library and the Homebrew library used by DXVK.
- Configure SDL before DXVK and supply a private generated `sdl3.pc` with the exact CMake library, matching headers and runtime search path. Both DXVK source modes depend on that shared target and clear stale Meson dependency discovery when reconfiguring.
- Removed the CI Homebrew SDL3 installation; kept the packager's conflicting-library rejection intact.
- A local CMake/Meson fixture deliberately offered a conflicting system SDL package. The private metadata took precedence, Meson linked the selected build and a version check ran successfully. Native product/standalone build and CTest remain green.

### Present Echelon with a concise README

- The owner explicitly requested Russian for the README. Translated all reader-facing content, retaining technical names and commands; this overrides the general English-only documentation rule for this file.

- Added the existing Echelon wordmark, a compact navigation row and one build-status badge.
- Kept the overview focused on the two games, local mods, independent profiles, setup and build commands. Moved implementation details behind existing guide links and retained attribution and licensing.
- Checked all relative links and section anchors. The owner requested this final documentation improvement, then completion of the merge and a stop without further work.

### Finish archive review and portable launcher builds

- Added a shared 1023-byte BIG entry-path limit for import validation and the native engine buffer. Engine parsing also rejects oversized or unterminated paths, including archives loaded outside the launcher.
- Restored recursive managed BIG/GIB discovery without following directory symlinks; extended both-engine smoke coverage with a nested, mixed-case GIB archive in every layer.
- Added import boundary regressions for maximum-length and oversized BIG paths. Native product and both standalone builds, CTest and both-engine nested-archive/precedence smoke checks pass.
- macOS product CI revealed that the root launcher inherited C++14 rather than the engine subdirectory standard. Both launcher and settings-test targets now explicitly require C++17 for `std::filesystem`.
- Exercised oversized and unterminated BIG names through legacy `-mod`, bypassing the installer: both engines reject them and return cleanly.

### Fix SagePatch after source relocation

- CI exposed a macOS compile failure: SagePatch guessed an SDL include path under the engine build directory, while FetchContent used the product build root. Replaced the path with `SDL3::Headers`, preserving macOS dynamic symbol lookup without linking another SDL library.
- Made the configured SagePatch target a launcher dependency and removed the Flatpak build-error suppression, so product packaging cannot silently omit the configured patch.
- The SagePatch target builds successfully in the local Distrobox; macOS is revalidated through CI.

### Resolve pre-merge file-handling findings

- The owner confirmed the two-branch arrangement and explicitly resumed the foundation merge into product `main`; the locked original `upstream` must remain unchanged.
- Validated three automated-review findings. Retail data import used destructive move helpers; it now copies and verifies files, preserves identical files and conflicts, and rejects overlapping destinations or directory links that redirect writes into the source.
- Extracted the copy-only retail importer for regression tests and removed unused destructive migration helpers. Both Generals and Zero Hour import paths use the same helper.
- Layer enumeration now resolves directory casing consistently with direct reads and returns logical paths, preserving the engine's root-INI-before-subdirectory loading rule. This shared fix applies to both engines.
- Passed incremental product/modules build, CTest regressions (source retention, repeated imports, conflicts, file links, destination isolation, case-insensitive enumeration and logical paths), module/build-boundary checks, and the both-engine content-stack smoke test.
- The current retail fixture root is `~/.Echelon`; the old `~/.GeneralsArsenal` directory no longer exists. Tests use temporary homes and read-only fixture inputs. The packaged build is being refreshed before final verification.

### Preserve an unchanged upstream branch

- The owner paused the pending merge and requested two permanent branches: product `main` and unchanged `upstream`. PR #1 was still unmerged and was returned to draft; the local merge/review observer was stopped.
- Created `origin/upstream` directly at original GeneralsX `3c2ed4589599beb631ea6da602c8ac95abf6bcdc`, with its original history and root layout. No Echelon commit or directory relocation is present in that branch.
- Protected the clean branch as read-only, including administrator pushes; disabled force pushes and deletion, and allowed upstream fork syncing.
- Documented the distinction between external remote `upstream`, clean branch `origin/upstream`, and integrated product directory `GeneralsX/`. Product integration still uses a reviewed subtree merge into a temporary branch based on `main`.
- The temporary foundation branch remains until the owner resumes the merge. No branch was deleted and no PR was merged.

### Align review automation

- Updated review path hints for the relocated engine and Echelon annotations. Added the product ownership context to the historical direct-reference sync prompt.
- Made CI summary fail if path detection fails, preventing a falsely successful run with all builds skipped.

### Verify the next upstream merge

- The synthetic subtree merge passed on `a3b91d86f`: upstream edits, a root addition and a deletion stayed under `GeneralsX/`, while the product tree remained unchanged. The test creates temporary Git objects without changing refs, the index or working files.
- Re-fetched upstream and confirmed the integrated SHA is still current. Product Python/shell/YAML syntax, identity/build boundaries and workspace paths pass final checks.

### Relocate the upstream engine and validate Echelon

- Followed the owner's explicit ordinary-directory choice: engine and upstream tools/docs are tracked under `GeneralsX/`; launcher, integration, identity, packaging, instructions and product QA remain at the root.
- Added explicit CMake source/binary roots and rebuilt all product and standalone targets from a fresh build directory. Verified launcher OFF and restored ON configurations, hosted flags and private exports.
- Created editable Echelon vector identity masters and packaged licensed DejaVu fonts. Corrected active workflow, build, task and documentation paths.
- Renamed the existing GitHub repository to `Cheviiot/Echelon`, preserving history/issues, and updated `origin`.
- Passed local content/settings and traced local mod UI checks, 20 native alternating sessions and 20 installed Flatpak sessions with window restoration. Built and installed the approximately 14 MiB unified Flatpak.
- Recorded replay failures honestly: two local replays fail in both hosted and standalone engines; six public upstream ZH captures mismatch at frame 0 using the available retail data. Cross-platform CRC remains unverified. No CRC checks were weakened.
- Prepared macOS unified packaging and CI; macOS execution is unavailable locally. CI reports replay tests as skipped when the asset key is absent. Wayland explicit-sync is skipped on the available compositor, which does not advertise it.
- Added the ownership/validation report and a synthetic subtree mapping check for future reviewed upstream merges.


### Integrate GeneralsX Beta 18 and current main

- Merged upstream `3c2ed4589599beb631ea6da602c8ac95abf6bcdc` (84 incoming commits). Engine, audio, deterministic math, graphics, filesystem, and save/load changes merged without textual conflicts.
- Combined both August diaries chronologically, retaining every contribution.
- Preserved the fork-owned universal release workflow and Linux installation instructions; upstream Windows/separate-game release packaging is outside the Echelon product scope.
- Runtime and build verification follows the product-layer extraction; the merge alone is not recorded as validated.

### Preserve the Arsenal baseline for Echelon

- The owner renamed the checkout to `Games/Echelon` and approved the Echelon foundation plan.
- Preserved all 115 pre-existing changed paths, binary changes, untracked files, checksums, and complete Git history in the workspace backup before changing source code.
- Updated the workspace registry to the existing Echelon checkout; no second repository or old-path symlink was created.
- The former remote mod catalog was deliberately deleted. Echelon will retain local mod import and management; reliable online distribution is deferred to a separate design.
- Created a source checkpoint so the existing launcher, module lifecycle, mod installer, localization, and packaging work remain recoverable during upstream integration.

### Establish the Echelon product layer before directory separation

- Extracted the launcher and shared engine entry implementation, separated hosted libraries and allocator flags from standalone engines, generated C++ identity from CMake, and removed online catalog UI and requests.
- Preserved local imports, installed versions, compatibility graphs, profiles, content layering, trash/restore, and isolated transport tests. Retired the old online-repository plan.
- Linux hosted and standalone builds completed in `ubuntu-dev` using fresh `build/linux64-echelon` and `build/linux64-standalone` directories.
- Passed settings/content tests, HTTPS resume/ETag/file-set fixtures, both headless profiles, local mod UI with network tracing, both-engine content stacks, and supervisor recovery.
- Completed 20 alternating sessions from each starting engine. All sessions reached quiescence and restored window state; the Ubuntu Mutter does not advertise explicit-sync, so that protocol check is explicitly skipped. Starting from Zero Hour: RSS growth 36,964 KiB after warmup, final 15 threads and 21 file descriptors.
- Prepared unified macOS packaging and CI changes; these remain unvalidated on macOS.
- The owner then requested physical source separation: keep product code at repository root and place the engine in an ordinary tracked `GeneralsX/` directory, with no submodule. Preserve this checkpoint before relocating source and regenerating build caches.
